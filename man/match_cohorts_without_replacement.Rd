% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_cohorts_without_replacement.R
\name{match_cohorts_without_replacement}
\alias{match_cohorts_without_replacement}
\title{Match Cohorts Without Replacement Using Greedy Matching}
\usage{
match_cohorts_without_replacement(
  matching_pop_groupkey = NULL,
  matching_vars = NULL,
  profile_table = NULL,
  matching_query = NULL,
  matching_conn = NULL,
  result_dir = NULL,
  result_file = NULL,
  save_output = FALSE,
  n_cores = NULL,
  exposed_batch_size = 50000L,
  start_seed = 42,
  col_person_id = "person_id",
  col_match_id = "match_id",
  col_treatment_group = "group",
  col_T0 = "T0",
  col_matching_status_start = "matching_status_start",
  col_matching_status_end = "matching_status_end",
  col_age_iterator = "year_of_birth"
)
}
\arguments{
\item{matching_pop_groupkey}{A data frame or data.table containing the eligible
matching population with at least person identifier, matching interval, year of birth,
group indicator, and \code{groupkey}. Typically this is the output of
\code{get_matching_population()}.}

\item{matching_vars}{A character vector of exact matching variable names to append
back to the final output via the lookup table.}

\item{profile_table}{A data frame or data.table containing one row per matching
profile and the associated \code{groupkey}. Typically this is the output of
\code{get_profile_table()}.}

\item{matching_query}{SQL query template used for greedy candidate generation.
The query should return accepted candidate pairs and may include the placeholder
string \verb{__START_SEED__}, which will be replaced by \code{start_seed}.}

\item{matching_conn}{A DuckDB connection used to create temporary matching tables
and execute the greedy candidate selection queries.}

\item{result_dir}{Directory where the output file will be written if
\code{save_output = TRUE}.}

\item{result_file}{Base filename for the saved output, without extension.}

\item{save_output}{Logical. If \code{TRUE}, writes the result to disk as a
\code{.parquet} file. If \code{FALSE}, returns the matched cohort as an object.}

\item{n_cores}{Integer number of DuckDB threads to use. Defaults to all detected
cores minus one.}

\item{exposed_batch_size}{Integer cap on the number of exposed spells processed
per SQL batch within each greedy round. Batching follows global exposed priority
order to reduce memory pressure while preserving no-replacement behavior.
Defaults to \code{50000}.}

\item{start_seed}{Integer seed used for deterministic tie-breaking in the greedy
matching order and control selection.}

\item{col_person_id}{Column name for the person identifier. Default is \code{"person_id"}.}

\item{col_match_id}{Column name for the match identifier in the returned output.
Default is \code{"match_id"}.}

\item{col_treatment_group}{Column name for the output treatment group label.
Default is \code{"group"}.}

\item{col_T0}{Column name for the output index date. Default is \code{"T0"}.}

\item{col_matching_status_start}{Column name for the matching interval start date.
Default is \code{"matching_status_start"}.}

\item{col_matching_status_end}{Column name for the matching interval end date.
Default is \code{"matching_status_end"}.}

\item{col_age_iterator}{Column name for the year-of-birth matching variable.
Default is \code{"year_of_birth"}.}
}
\value{
If \code{save_output = FALSE}, returns a data.table with one row per
output cohort record. Matched exposed and matched controls share the same
\code{match_id}; unmatched exposed spells are returned with \code{NA} in
\code{match_id} and group label \code{"UNMATCHED"}. If \code{save_output = TRUE},
the result is written to disk and not returned.
}
\description{
Performs greedy exposed-control matching without replacement using DuckDB-backed
SQL candidate generation and round-wise person-level pool reduction.
Exposed spells are prioritised by earliest \code{T0} (the exposed spell start
date), with deterministic seeded tie-breaking. Once a person is matched, that
person is removed from both the exposed and control pools for all subsequent rounds.
}
\details{
The algorithm proceeds in greedy rounds:
\enumerate{
\item Select all currently available exposed spells and order them by ascending
exposed start date, then apply seeded deterministic tie-breaking.
\item For each exposed spell, identify currently available control spells with
the same \code{groupkey}, compatible interval overlap, different person identifier,
and year of birth within exposed year ± 1.
\item Select one candidate control per exposed spell using seeded deterministic ranking.
\item Resolve collisions so that a control cannot be assigned to multiple exposed
spells and a person cannot appear more than once within the same round across
either role.
\item Remove all matched persons from both exposed and control pools.
\item Repeat until no additional matches can be formed.
}

Exposed spells that are never accepted as matched exposed records are returned as
\code{"UNMATCHED"}, including cases where the same person became unavailable because
they were matched first as a control.
}
\note{
This function implements matching without replacement only. It does not perform
bootstrap resampling. It is intended to be called through
\code{build_study_cohort(..., matching_mode = "without_replacement")}, although it can
also be used directly if the required intermediate objects and DuckDB connection
are already available.
}
\examples{
\dontrun{
matching_conn <- DBI::dbConnect(duckdb::duckdb(), "intermediate_outputs/matching.duckdb")

D4_MSC <- match_cohorts_without_replacement(
  matching_pop_groupkey = D3_MATCHING_POP,
  matching_vars = matching_vars,
  profile_table = D3_LOOKUP_TABLE,
  matching_query = matching_query,
  matching_conn = matching_conn,
  save_output = FALSE
)

DBI::dbDisconnect(matching_conn, shutdown = TRUE)
}

}
\seealso{
\code{\link[=build_study_cohort]{build_study_cohort()}}, \code{\link[=get_profile_table]{get_profile_table()}}, \code{\link[=get_matching_population]{get_matching_population()}}
}
