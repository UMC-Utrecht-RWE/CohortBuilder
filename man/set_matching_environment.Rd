% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_matching_environment.R
\name{set_matching_environment}
\alias{set_matching_environment}
\title{Set Up Matching Environment}
\usage{
set_matching_environment(
  eligible_pop = NULL,
  matching_query = NULL,
  with_bootstrap = FALSE,
  n_bootstraps = NULL,
  dir_bootstrap = NULL,
  dir_matching_db = NULL
)
}
\arguments{
\item{eligible_pop}{A file path to the eligible population data. Must be specified; otherwise, the function will stop.}

\item{matching_query}{A file path to the matching query. Must be specified; otherwise, the function will stop.}

\item{with_bootstrap}{Logical. If \code{TRUE}, enables bootstrapping functionality and manages bootstrap directories. Default is \code{FALSE}.}

\item{n_bootstraps}{Integer. Number of bootstrap iterations. Required when \code{with_bootstrap} is \code{TRUE}.}

\item{dir_bootstrap}{A directory path for storing bootstrap outputs. Required if \code{with_bootstrap} is \code{TRUE}.}

\item{dir_matching_db}{A file path to the matching database. If it exists, it will be removed during setup.}
}
\value{
\code{NULL}, invisibly. Called for its side-effects.
}
\description{
This function sets up the environment for the matching process by verifying input files,
creating necessary directories, and cleaning up old files or databases.
}
\details{
\itemize{
\item \strong{Eligibility Check}: The function ensures that \code{eligible_pop} and \code{matching_query} are provided and stops with an error if they are missing.
\item \strong{Bootstrapping}: If \code{with_bootstrap} is \code{TRUE}, the function:
\itemize{
\item Creates the bootstrap output folder if it does not exist.
\item Deletes existing bootstrap files matching the pattern \verb{*_bootstrap_*.parquet} in the specified directory.
}
\item \strong{Database Cleanup}: If a matching database file exists at the specified \code{dir_matching_db} path, it is deleted.
\item \strong{Logging}: Logs key actions, including directory creation, file deletion, and setup completion.
}
}
\examples{
\dontrun{

}

}
