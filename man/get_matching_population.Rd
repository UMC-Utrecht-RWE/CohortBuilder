% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_functions.R
\name{get_matching_population}
\alias{get_matching_population}
\title{Get the Eligible Matching Population}
\usage{
get_matching_population(
  eligible_pop = NULL,
  profile_table = NULL,
  matching_vars = NULL,
  save_output = NULL,
  output_dir = NULL,
  output_file = "D3_MATCHING_POP",
  col_person_id = "person_id",
  col_eligible_exposed = "eligible_exposed",
  col_eligible_control = "eligible_control",
  col_matching_status_start = "matching_status_start",
  col_matching_status_end = "matching_status_end",
  col_age_iterator = "year_of_birth"
)
}
\arguments{
\item{eligible_pop}{A data frame containing the eligible population with relevant columns for matching.}

\item{profile_table}{A data frame containing additional profile information for the population.}

\item{matching_vars}{A character vector of variable names used for joining \code{eligible_pop} with \code{profile_table}.}

\item{save_output}{A logical value. If \code{TRUE}, the resulting matching population is saved to the specified directory.}

\item{output_dir}{A string specifying the directory to save the output if \code{save_output} is \code{TRUE}.}

\item{output_file}{A string specifying the file name for the saved output. Defaults to \code{"D3_MATCHING_POP"}.}

\item{col_person_id}{A string specifying the column name for the unique person identifier. Defaults to \code{"person_id"}.}

\item{col_eligible_exposed}{A string specifying the column name indicating eligibility for the exposed group. Defaults to \code{"eligible_exposed"}.}

\item{col_eligible_control}{A string specifying the column name indicating eligibility for the control group. Defaults to \code{"eligible_control"}.}

\item{col_matching_status_start}{A string specifying the column name for the start date of the matching status. Defaults to \code{"matching_status_start"}.}

\item{col_matching_status_end}{A string specifying the column name for the end date of the matching status. Defaults to \code{"matching_status_end"}.}

\item{col_age_iterator}{A string specifying the column name for the year of birth or age iterator. Defaults to \code{"year_of_birth"}.}
}
\value{
A data.table of the matching population, with individuals grouped into "exposed" and "control" groups.
If \code{save_output} is \code{TRUE}, the function also saves the resulting data frame to the specified directory.
}
\description{
This function creates a subset of the eligible population, prepares the data for matching
by grouping individuals into exposed and control groups, and optionally saves the resulting
population to disk.
}
\details{
\itemize{
\item The function filters and joins the eligible population (\code{eligible_pop}) with the \code{profile_table}
based on the variables specified in \code{matching_vars}.
\item Individuals are assigned to either the "exposed" or "control" group based on their eligibility columns.
\item Additional grouping and formatting are performed to prepare the data for downstream matching.
\item If \code{save_output} is \code{TRUE}, the resulting population is saved in \code{.parquet} format for quick access.
}
}
\examples{
\dontrun{

}

}
