% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_eligibility_data.R
\name{generate_eligibility_data}
\alias{generate_eligibility_data}
\title{Generate Simulated Eligibility Data}
\usage{
generate_eligibility_data(
  n = 10000,
  start_seed = 42,
  save_output,
  output_dir,
  output_file = "D3_ELIGIBILITY",
  n_spells = 2
)
}
\arguments{
\item{n}{Integer. Number of rows to generate in the synthetic dataset. Default is 10,000.}

\item{start_seed}{Integer. Seed for reproducibility of random number generation. Default is 42.}

\item{save_output}{Logical. If TRUE, the generated dataset will be saved to the specified directory.}

\item{output_dir}{Character. Directory where the dataset should be saved if \code{save_output} is TRUE.}

\item{output_file}{Character. Name of the output file (without extension). Default is \code{"D3_ELIGIBILITY"}.}

\item{n_spells}{Integer. Number of spells per person. Default is 2.}
}
\value{
A data.table containing the generated synthetic dataset with detailed eligibility attributes.
}
\description{
This function generates a synthetic dataset to simulate eligibility data for analysis.
It creates attributes such as vaccination history, risk factors, and demographic details.
The dataset is customizable in size and can be saved to a specified directory.
}
\details{
The function generates various attributes, including:
\itemize{
\item \strong{person_id}: Unique identifier for individuals.
\item \strong{SV_REGION}: Regional information (categorical).
\item \strong{CDC_RISK}: CDC-defined risk levels.
\item \strong{SV_HIST_COVID_VACC}: Historical COVID vaccination count.
\item \strong{SV_BRAND_COVID_VACC}: Brand of COVID vaccine received.
\item \strong{SV_IMMUNOCOMPROMISED}: Immunocompromised status.
\item \strong{SV_PREG_STATUS}: Pregnancy status.
\item \strong{COMP_COMORBIDITIES}: Presence of comorbidities.
\item \strong{receives_any_covidvaccine}: Indicates if any COVID vaccine was received.
\item \strong{receives_bivalent}: Indicates if a bivalent vaccine was received.
\item \strong{start}: Start date of matching eligibility.
\item Additional derived variables like age, eligibility, and exposure status.
}

If \code{save_output} is TRUE, the generated dataset is saved in \code{.parquet} format in the \code{output_dir}.
}
\examples{
\dontrun{
# Generate a synthetic dataset with default parameters
dataset <- generate_eligibility_data()

# Generate and save a dataset
dataset <- generate_eligibility_data(
  n = 5000,
  start_seed = 123,
  save_output = TRUE,
  output_dir = "data/",
  output_file = "SyntheticEligibility"
)
}
}
