% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_cohorts_with_replacement.R
\name{match_cohorts_with_replacement}
\alias{match_cohorts_with_replacement}
\title{Match Cohorts with Replacement and Optional Bootstrapping}
\usage{
match_cohorts_with_replacement(
  matching_pop_groupkey = NULL,
  matching_vars = NULL,
  profile_table = NULL,
  matching_query = NULL,
  target_table_query = NULL,
  matching_conn = NULL,
  result_dir = NULL,
  result_file = NULL,
  save_output = FALSE,
  dir_bootstrap = NULL,
  with_bootstrap = FALSE,
  n_bootstraps = 500,
  n_cores = NULL,
  start_seed = 42,
  col_person_id = "person_id",
  col_match_id = "match_id",
  col_treatment_group = "group",
  col_T0 = "T0",
  col_matching_status_start = "matching_status_start",
  col_matching_status_end = "matching_status_end",
  col_age_iterator = "year_of_birth",
  age_offset = 1,
  col_date_match = NULL,
  date_match_offsets = NULL
)
}
\arguments{
\item{matching_pop_groupkey}{A data frame containing the eligible population to be matched, with a grouping key.}

\item{matching_vars}{A character vector of variable names to use in the matching process.}

\item{profile_table}{A data frame containing additional profile information for the population.}

\item{matching_query}{A SQL query string defining the matching logic.}

\item{target_table_query}{A SQL query string to create the target table in DuckDB.}

\item{matching_conn}{A DuckDB database connection object to facilitate SQL execution.}

\item{result_dir}{A string specifying the directory to save the final matched population results.}

\item{result_file}{A string specifying the file name for the final matched population results.}

\item{save_output}{A logical value. If \code{TRUE}, the matched results are saved to the specified file.}

\item{dir_bootstrap}{A string specifying the directory to save bootstrap results, if applicable.}

\item{with_bootstrap}{A logical value. If \code{TRUE}, bootstrapping is performed on the matching process.}

\item{n_bootstraps}{An integer specifying the number of bootstrap iterations. Defaults to 500.}

\item{n_cores}{An integer specifying the number of cores to use for SQL execution. Defaults to the total cores minus one.}

\item{start_seed}{An integer specifying the starting seed for reproducibility in bootstrapping. Defaults to 42.}

\item{col_person_id}{A string specifying the column name for the unique person identifier. Defaults to \code{"person_id"}.}

\item{col_match_id}{A string specifying the column name for the match identifier. Defaults to \code{"match_id"}.}

\item{col_treatment_group}{A string specifying the column name for the treatment group identifier. Defaults to \code{"group"}.}

\item{col_T0}{A string specifying the column name for the treatment initiation time. Defaults to \code{"T0"}.}

\item{col_matching_status_start}{A string specifying the column name for the start date of the matching status. Defaults to \code{"matching_status_start"}.}

\item{col_matching_status_end}{A string specifying the column name for the end date of the matching status. Defaults to \code{"matching_status_end"}.}

\item{col_age_iterator}{A string specifying the column name for the (numeric) year of birth or age iterator. Defaults to \code{"year_of_birth"}.}

\item{age_offset}{A numeric value specifying the range of values of col_age_iterator with which to select candidate controls. Defaults to 1. If no range-matching on this variable required, user should set to NULL.}

\item{col_date_match}{A character vector of date column names for range matching. Defaults to \code{NULL}.}

\item{date_match_offsets}{A named integer vector specifying the offset (in days) for each date column in \code{col_date_match}.
Names must match the column names. Defaults to \code{NULL}.}
}
\value{
If \code{with_bootstrap} is \code{FALSE} and \code{save_output} is \code{FALSE}, the function returns a data.table of the matched population.
If \code{save_output} is \code{TRUE}, the results are saved to the specified file.
}
\description{
This function performs matching between exposed and control populations within a study cohort,
with optional bootstrapping to account for variability in the results. Matching is performed
using a provided SQL query, leveraging DuckDB for efficient data handling. Matching is done by batches defined by unique values of col_age_iterator (by default year of birth) of the exposed which is assumed numeric.
Candidate control matches are selected based on the profile table, and having col_age_iterator value between col_age_iterator - age_offset and col_age_iterator + age_offset.
If no range matching on col_age_iterator is required, set age_offset to NULL. In that case, all candidate controls are selected
}
\details{
\itemize{
\item The function begins by configuring the DuckDB connection and setting the number of threads for execution.
\item If bootstrapping is enabled, the population is resampled with replacement in each iteration.
\item Matching is performed via a provided SQL query, adjusted for each year of birth or other specified iterators.
\item Results are saved either to a specified directory for bootstrapping or as a single output for the matched population.
\item Temporary tables and data structures are cleaned up at the end of each iteration to manage memory usage.
}
}
\note{
If \code{with_bootstrap} is \code{FALSE}, only one iteration is performed, overriding \code{n_bootstraps} if specified.
}
\examples{
\dontrun{

}

}
