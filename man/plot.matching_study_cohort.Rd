% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_study_cohort_methods.R
\name{plot.matching_study_cohort}
\alias{plot.matching_study_cohort}
\title{Plot Method for Matching Study Cohort}
\usage{
\method{plot}{matching_study_cohort}(x, y, by_var = "T0", ...)
}
\arguments{
\item{x}{A data.table with class "matching_study_cohort" (output from \code{build_study_cohort()}).}

\item{y}{Unused; provided for S3 generic compatibility.}

\item{by_var}{Character. Variable to plot on the category axis.
Default is \code{"T0"}.}

\item{...}{Additional arguments (unused).}
}
\value{
A ggplot2 plot object (if ggplot2 is installed), or a base R plot.
If ggplot2 is available, returns a ggplot object invisibly.
If ggplot2 is not available, generates a base R stacked bar/column plot.
}
\description{
Creates stacked-count plots for matched study cohort data.
}
\details{
By default, this produces a stacked histogram of matched episodes by
\code{group} over \code{T0} (index date), with automatic binning. You can also provide
any other variable in \code{by_var} to generate a stacked bar plot over that
variable's levels.
}
\examples{
\dontrun{
result <- build_study_cohort(...)
# Default: stacked histogram over T0
plot(result)

# Stacked bars over a user-specified variable
plot(result, by_var = "group")
}

}
