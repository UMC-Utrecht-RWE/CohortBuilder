% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_functions.R
\name{get_profile_table}
\alias{get_profile_table}
\title{Generate a Profile Table for Matching Variables}
\usage{
get_profile_table(
  eligible_pop = NULL,
  matching_vars = NULL,
  save_output = FALSE,
  output_dir = NULL,
  output_file = "D3_LOOKUP_TABLE"
)
}
\arguments{
\item{eligible_pop}{A data frame containing the eligible population, including columns for the matching variables.}

\item{matching_vars}{A character vector of variable names used to create the profile table.}

\item{save_output}{A logical value. If \code{TRUE}, the resulting profile table is saved to the specified directory.}

\item{output_dir}{A string specifying the directory to save the output if \code{save_output} is \code{TRUE}.}

\item{output_file}{A string specifying the file name for the saved output. Defaults to \code{"D3_LOOKUP_TABLE"}.}
}
\value{
A data.table containing the distinct combinations of matching variables and a unique integer identifier
(\code{groupkey}) for each combination. If \code{save_output} is \code{TRUE}, the profile table is saved to disk in \code{.parquet} format.
}
\description{
This function creates a lookup table (profile table) from the eligible population for the specified matching variables.
The table assigns a unique key (\code{groupkey}) to each distinct combination of matching variable values.
}
\details{
\itemize{
\item The function filters the eligible population (\code{eligible_pop}) to include only the specified \code{matching_vars}.
\item It ensures each combination of matching variable values is unique and assigns a unique integer key (\code{groupkey})
using \code{.I}.
\item If \code{save_output} is \code{TRUE}, the resulting table is saved in \code{.parquet} format for quick access.
}
}
\examples{
\dontrun{

}

}
