% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_study_cohort.R
\name{build_study_cohort}
\alias{build_study_cohort}
\title{Run Matched Study Cohort Pipeline}
\usage{
build_study_cohort(
  eligible_pop = NULL,
  matching_query = NULL,
  target_table_query = NULL,
  matching_vars = c("SV_SEX", "SV_REGION", "SV_HIST_COVID_VACC", "SV_BRAND_COVID_VACC",
    "SV_PREG_STATUS", "SV_IMMUNOCOMPROMISED", "CDC_RISK", "SV_SES_STATUS",
    "bivalent_type_received"),
  dir_matching_db =
    "transformations/T3_study_design/intermediate_data_file/matching.duckdb",
  n_cores = NULL,
  log_name = "log_build_study_cohort",
  output_pars = list(save_output = TRUE, dir_output = "outputs", output_file_name =
    "D4_MSC"),
  intermediate_output_pars = list(save_intermediate_outputs = TRUE,
    dir_intermediate_outputs = "intermediate_outputs", profile_table_name =
    "D3_LOOKUP_TABLE", matching_pop_name = "D3_MATCHING_POP"),
  bootstrap_pars = list(with_bootstrap = FALSE, n_bootstraps = 500, dir_bootstrap =
    "intermediate_outputs/bootstraps", start_seed = 42),
  input_column_names = list(col_person_id = "person_id", col_eligible_exposed =
    "eligible_exposed", col_eligible_control = "eligible_control",
    col_matching_status_start = "matching_status_start", col_matching_status_end =
    "matching_status_end", col_age_iterator = "year_of_birth"),
  output_column_names = list(col_person_id = "person_id", col_match_id = "match_id",
    col_treatment_group = "group", col_T0 = "T0"),
  age_offset = 1,
  matching_mode = "with_replacement"
)
}
\arguments{
\item{eligible_pop}{A data frame containing the eligible population (e.g., \code{D3_ELIGIBILITY}).}

\item{matching_query}{A string defining the matching SQL query. If \code{NULL}, empty,
or whitespace-only, the packaged default SQL query is loaded automatically based
on \code{matching_mode}.}

\item{target_table_query}{A string defining the DB table query that will be used to hold the birth-year looped matching results.
For \code{matching_mode = "with_replacement"}, if \code{NULL}, empty, or whitespace-only,
the packaged default target-table SQL is loaded automatically.}

\item{matching_vars}{A character vector of matching variables. Default includes
demographic and risk-related variables such as \code{c("SV_SEX", "SV_REGION", "SV_HIST_COVID_VACC", "SV_BRAND_COVID_VACC", "SV_PREG_STATUS", "SV_IMMUNOCOMPROMISED", "CDC_RISK", "SV_SES_STATUS", "bivalent_type_received")}.}

\item{dir_matching_db}{The file path for the temporary DuckDB database used for matching.
Default is \code{"transformations/T3_study_design/intermediate_data_file/matching.duckdb"}.}

\item{n_cores}{Number of CPU cores to use for parallel processing. Default is \code{NULL},
which uses all available cores minus one.}

\item{log_name}{Name of the log file. Default is \code{"log_build_study_cohort"}.}

\item{output_pars}{A list of output parameters:
\describe{
\item{\code{save_output}}{Logical. If \code{TRUE}, saves the final matched cohort to disk. Default is \code{TRUE}.}
\item{\code{dir_output}}{Directory where the output file will be saved.}
\item{\code{output_file_name}}{The name of the output file (without extension). Default is \code{"D4_MSC"}.}
}}

\item{intermediate_output_pars}{A list of parameters for intermediate outputs:
\describe{
\item{\code{save_intermediate_outputs}}{Logical. If \code{TRUE}, saves intermediate files. Default is \code{TRUE}.}
\item{\code{dir_intermediate_outputs}}{Directory for intermediate outputs.}
\item{\code{profile_table_name}}{Name of the profile table file. Default is \code{"D3_LOOKUP_TABLE"}.}
\item{\code{matching_pop_name}}{Name of the matching population file. Default is \code{"D3_MATCHING_POP"}.}
}}

\item{bootstrap_pars}{A list of parameters for bootstrapping:
\describe{
\item{\code{with_bootstrap}}{Logical. If \code{TRUE}, performs bootstrapping during matching. Default is \code{FALSE}.}
\item{\code{n_bootstraps}}{Number of bootstrap iterations. Default is \code{500}.}
\item{\code{dir_bootstrap}}{Directory for bootstrap files.}
\item{\code{start_seed}}{Random seed for reproducibility. Default is \code{42}.}
}}

\item{input_column_names}{A list specifying the column names in the input data:
\describe{
\item{\code{col_person_id}}{Column name for the unique person identifier. Default is \code{"person_id"}.}
\item{\code{col_eligible_exposed}}{Column name indicating eligibility as exposed. Default is \code{"eligible_exposed"}.}
\item{\code{col_eligible_control}}{Column name indicating eligibility as a control. Default is \code{"eligible_control"}.}
\item{\code{col_matching_status_start}}{Column name for the start date of matching eligibility. Default is \code{"matching_status_start"}.}
\item{\code{col_matching_status_end}}{Column name for the end date of matching eligibility. Default is \code{"matching_status_end"}.}
\item{\code{col_age_iterator}}{Column name for the age iterator. Default is \code{"year_of_birth"}.}
}}

\item{output_column_names}{A list specifying the column names for the output data:
\describe{
\item{\code{col_person_id}}{Column name for the unique person identifier. Default is \code{"person_id"}.}
\item{\code{col_match_id}}{Column name for the match ID. Default is \code{"match_id"}.}
\item{\code{col_treatment_group}}{Column name for the treatment group. Default is \code{"group"}.}
\item{\code{col_T0}}{Column name for the T0 variable. Default is \code{"T0"}.}
}}

\item{age_offset}{A numeric value specifying the range of values of col_age_iterator with which to select candidate controls. Defaults to 1. If no range-matching on this variable required, user should set to NULL.}

\item{matching_mode}{Matching strategy. Use \code{"with_replacement"} (default) for the
existing SQL matcher, or \code{"without_replacement"} for greedy no-replacement matching.}
}
\value{
A data.table containing the matched study cohort. If \code{output_pars$save_output} is \code{TRUE},
the data frame is also saved to disk as a \code{.parquet} file.
}
\description{
This function serves as a wrapper to perform matching for a matched study cohort,
with optional bootstrapping. It includes multiple steps such as setting up the
matching environment, generating matching variables, preparing the profile table,
obtaining the eligible matching population, and performing matching.
}
\details{
The function integrates several components of the matching pipeline:
\itemize{
\item Setting up the matching environment (including bootstrapping, if specified).
\item Preparing matching variables and profile tables.
\item Filtering and processing the eligible population.
\item Executing the matching process with optional bootstrapping.
}
}
\note{
If no eligible exposed individuals are available (\code{eligible_exposed = FALSE} for all rows),
the function terminates early and logs an error. Similarly, a warning is issued if no eligible
controls are found.
}
\examples{
\dontrun{

}

}
