% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_functions.R
\name{get_matching_variables}
\alias{get_matching_variables}
\title{Retrieve and Validate Matching Variables}
\usage{
get_matching_variables(
  eligible_pop,
  matching_vars = c("SV_SEX", "SV_REGION", "SV_HIST_COVID_VACC", "SV_BRAND_COVID_VACC",
    "SV_PREG_STATUS", "SV_IMMUNOCOMPROMISED", "CDC_RISK", "SV_SES_STATUS",
    "bivalent_type_received")
)
}
\arguments{
\item{eligible_pop}{A data frame containing the eligible population, including potential matching variables.}

\item{matching_vars}{A character vector of matching variable names to be retrieved and validated. Defaults to
\code{c("SV_SEX", "SV_REGION", "SV_HIST_COVID_VACC", "SV_BRAND_COVID_VACC", "SV_PREG_STATUS", "SV_IMMUNOCOMPROMISED", "CDC_RISK", "SV_SES_STATUS", "bivalent_type_received")}.}
}
\value{
A character vector of matching variables that are present in the input data (\code{eligible_pop}).
If some variables are not found, they are excluded, and a warning is logged.
}
\description{
This function retrieves and validates the specified matching variables from the input eligible population.
It ensures only variables present in the data are used for matching, and logs warnings for any missing variables.
}
\details{
\itemize{
\item The function checks the presence of each variable in \code{matching_vars} within the columns of \code{eligible_pop}.
\item Any variables in \code{matching_vars} that are missing from the input data are excluded from the result,
and a warning is logged to inform the user.
\item The final list of variables to be used for matching is logged and returned.
}
}
\examples{
\dontrun{

}

}
